# Logger Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar `get_logger(name)` usando `structlog` sobre `logging.config.dictConfig`, con configuración completa en `logging.yaml`, output JSON compatible con Grafana Loki, y captura de errores opcional via Sentry.

**Architecture:** `logging.yaml` define handlers, formatters y niveles via `dictConfig` (estándar Python). `structlog` actúa como renderer encima de `logging`, emitiendo JSON estructurado o pretty-print según `LOG_FORMAT`. Sentry se activa si `SENTRY_DSN` está en el entorno. `logger.py` expone `get_logger(name)`, `set_logger_level`, `disable_logger`, `enable_logger`.

**Tech Stack:** `structlog>=24`, `PyYAML>=6`, `sentry-sdk>=2`, Python 3.14.

---

## Observability Stack

| Herramienta | Rol | Integración |
|-------------|-----|-------------|
| **Grafana Loki** | Agrega y consulta logs | Lee JSON por stdout — sin código extra |
| **Sentry** | Captura excepciones con contexto | `sentry-sdk` activo si `SENTRY_DSN` en env |
| **Prometheus** | Métricas numéricas | Fuera del scope — tarea futura |

---

## File Structure

| File | Action | Responsibility |
|------|--------|----------------|
| `src/shared/infrastructure/logger.py` | Create | Factory + utilidades de control en runtime |
| `logging.yaml` | Create | Config completa: handlers, formatters, niveles, campos extra |
| `tests/unit/shared/infrastructure/test_logger.py` | Create | Tests del factory y utilidades |
| `pyproject.toml` | Modify | Agregar `structlog`, `pyyaml`, `sentry-sdk` |

---

## Task 1: Agregar dependencias

**Files:**
- Modify: `pyproject.toml`

- [ ] **Step 1: Instalar dependencias**

```bash
uv add structlog pyyaml sentry-sdk
```

- [ ] **Step 2: Verificar pyproject.toml**

```toml
[project]
dependencies = [
    "structlog>=24.0",
    "pyyaml>=6.0",
    "sentry-sdk>=2.0",
]
```

---

## Task 2: Crear `logging.yaml`

**Files:**
- Create: `logging.yaml` (raíz del proyecto)

- [ ] **Step 1: Escribir el archivo**

```yaml
# logging.yaml — configuración de logging para sana-airlytics
# Procesado por logging.config.dictConfig + structlog como renderer
# LOG_FORMAT=json|pretty controla el output (default: pretty)
# LOG_LEVEL=DEBUG|INFO|WARNING|ERROR|CRITICAL (default: INFO)

version: 1
disable_existing_loggers: false

# Campos que structlog agrega a CADA log automáticamente
extra_fields:
  app: sana-airlytics
  environment: development

loggers:
  # Silenciar librerías ruidosas
  urllib3:
    level: WARNING
  httpx:
    level: WARNING

root:
  level: INFO
  handlers: [console]

handlers:
  console:
    class: logging.StreamHandler
    stream: ext://sys.stdout
    formatter: structlog

formatters:
  structlog:
    (): structlog.stdlib.ProcessorFormatter
    processor: structlog.dev.ConsoleRenderer
    foreign_pre_chain:
      - structlog.contextvars.merge_contextvars
      - structlog.stdlib.add_logger_name
      - structlog.stdlib.add_log_level
      - structlog.processors.TimeStamper
```

---

## Task 3: Implementar `logger.py`

**Files:**
- Create: `src/shared/infrastructure/logger.py`
- Test: `tests/unit/shared/infrastructure/test_logger.py`

- [ ] **Step 1: Escribir los tests primero**

```python
# tests/unit/shared/infrastructure/test_logger.py
from __future__ import annotations

import logging

import structlog

from src.shared.infrastructure.logger import (
    disable_logger,
    enable_logger,
    get_logger,
    set_logger_level,
)


class TestGetLogger:
    def test_returns_bound_logger(self):
        log = get_logger(__name__)
        assert isinstance(log, structlog.stdlib.BoundLogger)

    def test_different_names_return_different_loggers(self):
        log1 = get_logger("module.a")
        log2 = get_logger("module.b")
        assert log1 is not log2

    def test_logger_can_log_without_error(self):
        log = get_logger(__name__)
        log.info("test_event", key="value")
        log.warning("test_warning", detail="something")
        log.error("test_error", code=42)

    def test_name_is_optional(self):
        log = get_logger()
        assert isinstance(log, structlog.stdlib.BoundLogger)


class TestLoggerControls:
    def test_set_logger_level(self):
        set_logger_level("test.module", logging.DEBUG)
        assert logging.getLogger("test.module").level == logging.DEBUG

    def test_disable_and_enable_logger(self):
        disable_logger("test.disable")
        assert logging.getLogger("test.disable").disabled is True

        enable_logger("test.disable")
        assert logging.getLogger("test.disable").disabled is False
```

- [ ] **Step 2: Correr tests — verificar que fallan**

```bash
uv run pytest tests/unit/shared/infrastructure/test_logger.py -v
```
Esperado: `ImportError` porque `logger.py` está vacío.

- [ ] **Step 3: Implementar `logger.py`**

```python
from __future__ import annotations

import logging
import logging.config
import os
from pathlib import Path

import structlog
import yaml

_ROOT = Path(__file__).parents[4]


def _load_yaml_config() -> dict:
    path = _ROOT / "logging.yaml"
    if not path.exists():
        return {}
    with path.open() as f:
        return yaml.safe_load(f) or {}


def _init_sentry() -> None:
    dsn = os.getenv("SENTRY_DSN")
    if not dsn:
        return
    import sentry_sdk
    from sentry_sdk.integrations.logging import LoggingIntegration

    sentry_sdk.init(
        dsn=dsn,
        integrations=[
            LoggingIntegration(
                level=logging.ERROR,
                event_level=logging.ERROR,
            ),
        ],
        traces_sample_rate=0.0,
    )


def _configure() -> None:
    config = _load_yaml_config()
    fmt = os.getenv("LOG_FORMAT", config.get("format", "pretty")).lower()
    level_name = os.getenv("LOG_LEVEL", "INFO").upper()
    extra_fields: dict = config.get("extra_fields", {})

    renderer = (
        structlog.processors.JSONRenderer()
        if fmt == "json"
        else structlog.dev.ConsoleRenderer(colors=True)
    )

    shared_processors: list = [
        structlog.contextvars.merge_contextvars,
        structlog.stdlib.add_logger_name,
        structlog.stdlib.add_log_level,
        structlog.processors.TimeStamper(fmt="iso", utc=True),
    ]

    if extra_fields:
        def _add_extra(_logger, _method, event_dict):  # noqa: ANN001
            event_dict.update(extra_fields)
            return event_dict
        shared_processors.insert(0, _add_extra)

    structlog.configure(
        processors=[
            *shared_processors,
            structlog.stdlib.ProcessorFormatter.wrap_for_formatter,
        ],
        wrapper_class=structlog.stdlib.BoundLogger,
        context_class=dict,
        logger_factory=structlog.stdlib.LoggerFactory(),
        cache_logger_on_first_use=True,
    )

    # Aplicar config de logging.yaml via dictConfig si existe
    if "version" in config:
        logging.config.dictConfig(config)
    else:
        logging.basicConfig(level=getattr(logging, level_name, logging.INFO))

    # Reemplazar formatter del handler con structlog
    formatter = structlog.stdlib.ProcessorFormatter(
        processor=renderer,
        foreign_pre_chain=shared_processors,
    )
    for handler in logging.getLogger().handlers:
        handler.setFormatter(formatter)

    _init_sentry()


_configure()


def get_logger(name: str | None = None) -> structlog.stdlib.BoundLogger:
    """Retorna un logger structlog. Sin nombre retorna el root logger."""
    return structlog.stdlib.BoundLogger(
        logging.getLogger(name),
        processors=[],
        context={},
    )


def set_logger_level(logger_name: str, level: int) -> None:
    """Cambia dinámicamente el nivel de un logger en runtime."""
    logging.getLogger(logger_name).setLevel(level)


def disable_logger(logger_name: str) -> None:
    """Deshabilita temporalmente un logger."""
    logging.getLogger(logger_name).disabled = True


def enable_logger(logger_name: str) -> None:
    """Habilita un logger previamente deshabilitado."""
    logging.getLogger(logger_name).disabled = False
```

- [ ] **Step 4: Correr tests — verificar que pasan**

```bash
uv run pytest tests/unit/shared/infrastructure/test_logger.py -v
```
Esperado: 6 PASSED.

- [ ] **Step 5: Verificar ruff**

```bash
uv run ruff check src/shared/infrastructure/logger.py
```
Esperado: sin errores.

- [ ] **Step 6: Commit**

```bash
git add src/shared/infrastructure/logger.py logging.yaml tests/unit/shared/infrastructure/test_logger.py pyproject.toml uv.lock
git commit -m "feat: add structured logger with yaml config, loki-compatible json and optional sentry"
```

---

## Task 4: Verificación manual

- [ ] **Step 1: Probar output pretty**

Crear `intento.py` temporal:
```python
from src.shared.infrastructure.logger import get_logger

log = get_logger(__name__)
log.info("sensor_reading", device_id="abc-123", value=42.5, unit="PM2.5")
log.warning("value_near_limit", device_id="abc-123", value=950.0)
log.error("mqtt_connection_failed", rc=5)
```

```bash
uv run python intento.py
```
Esperado: output coloreado con timestamp ISO, level, logger name, y campos `app`/`environment`.

- [ ] **Step 2: Probar output JSON (Loki-compatible)**

```bash
LOG_FORMAT=json uv run python intento.py
```
Esperado: líneas JSON con `timestamp`, `level`, `logger`, `event`, `app`, `environment`.

- [ ] **Step 3: Limpiar**

```bash
rm intento.py
```
